<%= form_for(@law) do |f| %>
  <% if @law.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@law.errors.count, "error") %> prohibited this law from being saved:</h2>

      <ul>
      <% @law.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mdl-textfield mdl-js-textfield">
    <%= f.label :description, "Descrição", class: "mdl-textfield__label", rows: "3", id: "sample5" %><br>
    <%= f.text_area :description, class: "mdl-textfield__input", for: "sample5" %>
  </div>
  <div class="field">
    <%= f.label :politician_id, "Político" %><br>
    <% politicians =  Politician.all.map{ |f| [f.name, f.id] } %>
    <%= f.select :politician_id, options_for_select(politicians) %><br>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
